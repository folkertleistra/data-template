# The layout files contain the html content of each tab. Add more if you want more tabs. The files should contain
# the html code needed for the page + the callbacks that are present on that page

import dash_html_components as html

layout = [html.Div('Hello World')]